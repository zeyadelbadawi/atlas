/**
 * Website sections — public entry point.
 */
export { SectionRenderer } from './SectionRenderer';
export {
  SECTION_METADATA,
  listSectionMetadata,
  getDefaultSectionConfig,
} from './section-metadata.registry';
export type { SectionMetadataEntry } from './section-metadata.registry';
