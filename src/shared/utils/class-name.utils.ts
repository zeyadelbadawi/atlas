/**
 * Class name composition.
 *
 * Re-exported from the shared utility layer so components import `cn` from
 * `@utils` alongside the rest of the shared helpers, keeping import paths
 * consistent across the codebase.
 */
export { cn } from '@/lib/utils';